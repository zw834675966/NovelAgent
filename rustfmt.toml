# rustfmt — AI 不可协商的格式门禁（cargo fmt --all --check）
# 保持接近默认，避免风格战争；只固定跨工具一致项。
edition = "2024"
max_width = 100
use_small_heuristics = "Default"
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
